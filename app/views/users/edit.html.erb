<main class="content">
  <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
<%= render 'form' %>

<%= link_to 'Cancelar', current_user, :class => "black_link" %>
</main>